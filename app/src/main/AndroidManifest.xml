<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.aalto.rtda">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BluetoothTrainingActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_bluetooth_training"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MeasuringActivity"
            android:label="@string/title_activity_measure_site_select"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RealtimeChartActivity"
            android:label="@string/title_activity_realtime_chart"
            android:theme="@style/AppTheme.NoActionBar">
            <receiver android:name=".RealtimeDataBroadcastReceiver">
                <intent-filter>
                    <action android:name="fi.aalto.rtda.RealtimeChartActivity.INTENT_ACTION_DEVICE_NUMBER"/>
                    <action android:name="fi.aalto.rtda.RealtimeChartActivity.INTENT_ACTION_NEW_RECORD"/>
                </intent-filter>
            </receiver>
        </activity>

        <service
            android:name=".MeasuringBackgroundService"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>